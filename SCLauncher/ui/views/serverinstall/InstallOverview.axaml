<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:serverinstallmodel="clr-namespace:SCLauncher.model.serverinstall"
             xmlns:serverinstallviews="clr-namespace:SCLauncher.ui.views.serverinstall"
             xmlns:design="clr-namespace:SCLauncher.ui.design"
             xmlns:li="using:LoadingIndicators.Avalonia"
             xmlns:converters="clr-namespace:SCLauncher.ui.converters"
             mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="300"
             x:Class="SCLauncher.ui.views.serverinstall.InstallOverview"
             x:DataType="serverinstallmodel:ServerInstallParams">
	
	<Design.DataContext>
		<design:DServerInstallParams/>
	</Design.DataContext>
	
	<UserControl.Styles>
		<Style Selector=":is(Control).Key">
			<Setter Property="Margin" Value="0 0 10 5"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
		<Style Selector="Label.Key">
			<Setter Property="Foreground" Value="{StaticResource Highlight}"/>
			<Setter Property="HorizontalContentAlignment" Value="Left"/>
			<Setter Property="Padding" Value="0 3"/>
			<Setter Property="FontSize" Value="16"/>
		</Style>
		<Style Selector=":is(Control).Value">
			<Setter Property="Margin" Value="0 0 0 5"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
		</Style>
		<Style Selector=":is(TextBlock).Value">
			<Setter Property="TextWrapping">Wrap</Setter>
		</Style>
	</UserControl.Styles>
	
	<DockPanel Margin="0 10 0 5">
		
		<!-- Installation properties -->
		<Grid ColumnDefinitions="Auto *"
		      RowDefinitions="Auto, Auto, Auto"
		      VerticalAlignment="Center"
		      DockPanel.Dock="Top">

			<Label Grid.Row="0" Grid.Column="0" Content="Install method" Classes="Key"/>
			<SelectableTextBlock Grid.Row="0" Grid.Column="1" Classes="Value" Text="{Binding Method}"/>
			
			<Label Grid.Row="1" Grid.Column="0" Content="Install path" Classes="Key"
			       IsVisible="{Binding #InstallPathText.IsVisible}"/>
			<SelectableTextBlock Grid.Row="1" Grid.Column="1" Name="InstallPathText" Classes="Value"
			           Text="{Binding Path, Mode=OneTime}"
			           IsVisible="{Binding Method, Converter={x:Static ObjectConverters.Equal},
						ConverterParameter={x:Static serverinstallmodel:ServerInstallMethod.External}}"/>
			
			<Label Grid.Row="2" Grid.Column="0" Content="Addons preset" Classes="Key"/>
			<ComboBox Grid.Row="2" Grid.Column="1" Name="PresetComboBox" Classes="Value" HorizontalAlignment="Stretch"
			          PlaceholderText="All"
			          DisplayMemberBinding="{Binding Converter={x:Static converters:EnumDescriptionConverter.Instance},
			           TargetNullValue='All'}"/>
		</Grid>
		
		<!-- Installation components -->
		<ScrollViewer AllowAutoHide="False" DockPanel.Dock="Bottom" Name="ComponentsScrollViewer" Classes="RightMargin">
			<ItemsControl Name="ComponentsGrid" ItemsSource="{Binding}" Margin="1 0">
				<ItemsControl.ItemTemplate>
					<DataTemplate DataType="serverinstallviews:ComponentViewModel">
						<DockPanel Margin="0 2">
							
							<DockPanel DockPanel.Dock="Right">
								<SelectableTextBlock DockPanel.Dock="Right" VerticalAlignment="Center" TextWrapping="Wrap"
								                     Text="{Binding Text}"/>
							</DockPanel>
							
							<CheckBox DockPanel.Dock="Left" VerticalAlignment="Top"
							          IsChecked="{Binding Enabled}"
							          IsEnabled="{Binding Editable}">
							          
								<TextBlock TextWrapping="Wrap" VerticalAlignment="Center">
									<TextBlock.Effect>
										<DropShadowEffect BlurRadius="15" Color="#80151515"/>
									</TextBlock.Effect>
									<Run FontSize="14" Foreground="{DynamicResource TextControlForeground}" Text="{Binding Component.Title}"/>
									<Run FontSize="12" Foreground="#BEBEBE" FontWeight="DemiBold" Text="{Binding Component.Description}"/>
								</TextBlock>
							</CheckBox>
							
							<Border Classes="DashedUnderline" Padding="0 10" VerticalAlignment="Bottom"/>

						</DockPanel>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>
		
		<!-- Installation components loading indicator -->
		<li:LoadingIndicator Name="LoadingIndicator" IsActive="True" Mode="DoubleBounce" SpeedRatio="1.5"
		                     Foreground="{StaticResource Highlight}"
		                     MaxHeight="150"
		                     VerticalAlignment="Bottom"/>
		
	</DockPanel>
</UserControl>
